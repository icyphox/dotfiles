#!/usr/bin/env bash

n=6; output=$(tr -cd '[:alnum:]' < /dev/urandom | head -c$n)
import -window root ~/pics/scrots/$output.png
notify-send 'screenshot taken!'
xclip -selection clipboard -t image/png -i ~/pics/scrots/$output.png
