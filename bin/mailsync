#!/bin/sh
# mailsync: sync email using mbsync and notify on new mail

mbsync -qa &>/dev/null

new="$HOME/mail/personal/Inbox/new/"
count="$(find $new -type f | wc -l)"

[[ "$count" -gt 0 ]] &&
    printf 'SEC:5\tTAG:mail\tMail\t%s new mail\n' "$count" > "$XNOTIFY_FIFO"
