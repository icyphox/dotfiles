#!/bin/sh
# signal: launch signal-desktop via a vm (vmm(4))

status="$(vmctl status 1 | grep running)"
[[ "$status" == "" ]] && {
    vmctl start pantwo
    sleep 10
}

ssh -Y pantwo signal-desktop &> /dev/null
